spec:
  template:
    spec:
      containers:
        - name: tiller
          env:
          - name: KUBERNETES_SERVICE_HOST
            value: kubemaster.svc.codfw.wmnet
          - name: KUBERNETES_SERVICE_PORT
            value: "6443"
