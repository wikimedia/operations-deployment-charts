apiVersion: v1
kind: Secret
metadata:
  name: {{ template "$SERVICE_NAME.releasename" . }}-secret-config
  labels:
    app: {{ template "$SERVICE_NAME.chartname" . }}
    chart: {{ template "$SERVICE_NAME.chartid" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data: {{- range $k, $v := .Values.config.private -}}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end -}}
