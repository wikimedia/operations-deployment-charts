Thank you for installing {{ template "mobileapps.chartname" . }}.

Your release is named {{ template "mobileapps.releasename" . }}.

To learn more about the release, try:

  $ helm status {{ template "mobileapps.releasename" . }}
  $ helm get {{ template "mobileapps.releasename" . }}

{{- if eq .Values.service.deployment "minikube" }}
You are using minikube, which means it's really easy to access your service. Run the following
MINIKUBE_HOST=$(minikube ip)
SERVICE_PORT=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ template "mobileapps.releasename" . }} -o jsonpath='{.spec.ports[0].nodePort}')

curl http://${MINIKUBE_HOST}:${SERVICE_PORT}/

or alternatively in your browser using the following URL

echo http://${MINIKUBE_HOST}:${SERVICE_PORT}/

{{- end }}
