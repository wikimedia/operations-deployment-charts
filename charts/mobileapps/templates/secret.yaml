apiVersion: v1
kind: Secret
metadata:
  name: {{ template "mobileapps.releasename" . }}-secret-config
  labels:
    app: {{ template "mobileapps.chartname" . }}
    chart: {{ template "mobileapps.chartid" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data: {{- range $k, $v := .Values.config.private -}}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end -}}
