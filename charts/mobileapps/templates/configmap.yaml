{{- if .Values.monitoring.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "mobileapps.releasename" . }}-metrics-config
  labels:
    app: {{ template "mobileapps.chartname" . }}
    chart: {{ template "mobileapps.chartid" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  prometheus-statsd.conf: |-
{{ .Files.Get "config/prometheus-statsd.conf" | indent 4 }}
{{ end }}
{{ template "tls.config" . }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: config-{{ .Release.Name }}
data:
  config.yaml: |- {{ include "config.app" . | indent 4 }}
