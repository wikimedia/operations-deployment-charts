mercurius:
  enabled: true
  debug: true
  jobs: ["mytestjob1", "mytestjob2"]

# These are enabled to check the pod.kubernetes.io/sidecars annotation
cache:
  mcrouter:
    enabled: true
mw:
  httpd:
    enabled: false
  logging:
    rsyslog: true
