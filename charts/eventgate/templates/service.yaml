{{ if not (hasSuffix "canary" .Release.Name) }}
{{/*
#
# Deploy a k8s Service for {{ template "wmf.releasename" . }}
# routing to all pods (in the current k8s namespace) that are labeled with:
#   chart: {{ template "wmf.chartname" . }}
#   app: {{ .Values.main_app.name }}
#   routed_via: {{ .Values.routed_via | default .Release.Name }}
#
*/}}
{{ if .Values.tls.enabled }}
{{ include "tls.service" . }}
{{- else }}
{{/*
# Only enable the main app http port if we are not using TLS.
# https://phabricator.wikimedia.org/T255871
*/}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "wmf.releasename" . }}-http-service
  labels:
    chart: {{ template "wmf.chartname" . }}
    app: {{ .Values.main_app.name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: NodePort
  selector:
    chart: {{ template "wmf.chartname" . }}
    app: {{ .Values.main_app.name }}
    routed_via: {{ .Release.Name }}
  ports:
  - name: {{ .Values.main_app.name }}-http
    protocol: TCP
    port: {{ .Values.main_app.port }}
    {{/* If the port is passed as a value use it, otherwise autoallocate */}}
    {{- if .Values.service.port -}}
    nodePort: {{ .Values.service.port }}
    {{- end }}
{{- end }}

{{- if .Values.debug_mode_enabled | default false }}
---
# debug_mode_enabled is true. Allow connections to Node Inspector.
apiVersion: v1
kind: Service
metadata:
  name: {{ template "wmf.releasename" . }}-debug-service
  labels:
    chart: {{ template "wmf.chartname" . }}
    app: {{ .Values.main_app.name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: NodePort
  selector:
    chart: {{ template "wmf.chartname" . }}
    app: {{ .Values.main_app.name }}
    routed_via: {{ .Values.routed_via | default .Release.Name }}
  - name: node-inspector-master
    protocol: TCP
    port: 9229
    nodePort: 31229
  - name: node-inspector-worker
    protocol: TCP
    port: 9230
    nodePort: 31230
{{- end }}

{{- else }}
{{/*
#
# Skipping deployment of k8s Service for 'canary' release {{ template "wmf.releasename" . }}.
# https://phabricator.wikimedia.org/T282148
#
*/}}
{{- end }}
