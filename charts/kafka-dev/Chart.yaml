apiVersion: v2
description: >
  A Helm chart for installing a single node Kafka (and Zookeeper)
  pod for development and testing purposes.
name: kafka-dev
version: 0.2.0
# skip ci checks for this Chart.yaml
annotations:
  skip-checks: "team,maintainers,home,sources"
