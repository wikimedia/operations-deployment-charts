tls:
  enabled: true
  image_version: 1.13.1-1 # latest as of 20200507
  public_port: 4969 # the port where TLS will be exposed
  certs:
    cert: |-
      -- begin --
      12345
      -- end --
    key: |-
      -- begin --
      678910
      -- end --
  # Enable telemetry
  telemetry:
    enabled: true
    port: 9361
