---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "eventstreams.releasename" . }}-config
data:
  config.yaml: |- {{ include "config.app" . | nindent 4  }}
{{- if .Values.main_app.kafka_ca_cert }}
  kafka_ca.crt.pem: |-
    {{ .Values.main_app.kafka_ca_cert | nindent 4 }}
{{ end }}
{{- if .Values.tls.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "eventstreams.releasename" . }}-tls-proxy-certs
  labels:
    app: {{ template "eventstreams.chartname" . }}
    chart: {{ template "eventstreams.chartid" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  service.crt: |-
{{ .Values.tls.certs.cert | indent 4 }}
  service.key: |-
{{ .Values.tls.certs.key | indent 4 }}
{{ end -}}
