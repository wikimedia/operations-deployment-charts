main_app:
  image: wikimedia/mediawiki-services-wikifeeds
  version: latest # we use latest everywhere in the defaults.
  port: 8889
service:
  port:
    name: http
    targetPort: 8889
    port: 8889
    nodePort: null

app:
  port: 8889
