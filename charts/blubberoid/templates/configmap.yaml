{{- if .Values.main_app.policy.enabled }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-policy
data:
  policy.yaml: |-
{{ include "policy.app" . | indent 4 }}
...
{{- end -}}
