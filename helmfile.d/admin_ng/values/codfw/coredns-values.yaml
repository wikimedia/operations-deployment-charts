replicaCount: 12

resources:
  requests:
    cpu: 1
    memory: 500Mi
  limits:
    memory: 500Mi

service:
  clusterIP: 10.192.72.3

# Unset the image tag to use the default image from the chart
image:
  tag: ""
