# This is before coredns works, we can't rely on internal DNS, so use the external one
kubernetesApi:
  host: ml-staging-ctrl.svc.codfw.wmnet
  port: 6443

replicaCount: 4

service:
  clusterIP: 10.194.62.3
