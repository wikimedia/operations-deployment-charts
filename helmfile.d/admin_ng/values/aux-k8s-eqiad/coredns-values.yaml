# This is before coredns works, we can't rely on internal DNS, so use the external one
kubernetesApi:
  host: 'aux-k8s-ctrl.svc.eqiad.wmnet'
  port: 6443

replicaCount: 4

service:
  clusterIP: '10.67.64.3'
