config:
  airflow:
    instance_name: search
  extra_secrets:
    wikimedia_enterprise.yaml: |
      username: {{ $.Values.config.wikimedia_enterprise.username }}
      password: {{ $.Values.config.wikimedia_enterprise.password }}

ingress:
  gatewayHosts:
    default: "airflow-search"
    extraFQDNs:
    - airflow-search.wikimedia.org
