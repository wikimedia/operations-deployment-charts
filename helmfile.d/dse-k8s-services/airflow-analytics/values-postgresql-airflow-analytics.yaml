cluster:
  initdb:
    import:
      host: an-db1001.eqiad.wmnet
      user: airflow_analytics
      dbname: airflow_analytics
  cluster:
    storage:
      size: 30Gi

external_services:
  postgresql: [analytics]