app:
  config_files:
    app.config.properties:
      pipeline.name: cirrus-streaming-updater-producer-codfw
      topic-prefix-filter: 'codfw'
      kafka-source-config.group.id: cirrus-streaming-updater-producer-codfw
