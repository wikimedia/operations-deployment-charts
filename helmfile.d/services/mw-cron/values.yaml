mwcron:
  enabled: true

mw:
  httpd:
    enabled: false
  logging:
    rsyslog: true
  helpers:
    feature_flags:
      periodic:
        networkpolicy: false

# These are enabled to check the pod.kubernetes.io/sidecars annotation
cache:
  mcrouter:
    enabled: true

mesh:
  enabled: true
  public_port: ~

monitoring:
  enabled: false
  statsd: ~
