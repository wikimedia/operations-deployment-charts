resources:
  # When ramping up replicas in the main release, please remember
  # to raise the number of canary replicas as well.
  # We aim for ~3% of the deployment for canaries, rounding up.
  # T383845 - Serve ~ 25% of traffic on PHP 8.1 (main: 108 migration: 34)
  # Original size: 141
  replicas: 108

mesh:
  public_port: 4452
  tracing:
    enabled: true
    sampling: 0.1

monitoring:
  statsd: ~
