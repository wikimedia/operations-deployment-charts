# Value overrides to be used when testing.
# For local environment specific overrides, create a copy called
# values-test.local.yaml and adjust as needed.

smokepy:
  gateway:
    target_url: http://localhost:8087