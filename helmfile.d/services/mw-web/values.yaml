resources:
# When ramping up replicas in the main release, please remember
# to raise the number of canary replicas as well.
# We aim for ~3% of the deployment for canaries, rounding up.
  # Normal day to day multi-DC replica count
  replicas: 293

  # Estimated sizing for single DC *with* a 70/30 split with
  # mw-web-next, see r/1122561 for details.
  # replicas: 315

mesh:
  public_port: 4450
  tracing:
    enabled: true
    sampling: 0.01

php:
  apc:
    size: 2048

monitoring:
  statsd: ~
