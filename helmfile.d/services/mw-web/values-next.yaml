resources:
  # T383845 - 118 replicas is ~ 40% of the size of main (293) as of 2025-02-28.
  #replicas: 118
  replicas: 95  # drop by 20% while troubleshooting deployment failures

# TODO: T383845 - Temporarily increase maxUnavailable. Needs revised down as
# the deployment grows.
strategy:
  rollingUpdate:
    maxUnavailable: 20%

mesh:
  public_port: 4454
  tracing:
    service_name: mw-web-next

php:
  version: "8.1"
