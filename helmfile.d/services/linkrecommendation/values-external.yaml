cronJobEnabled: false
resources:
  replicas: 1
tls:
  public_port: 4006
config:
  public:
    # Needed so that the Swagger UI can load assets correctly when served from
    # https://api.wikimedia.org/service/linkrecommendation/
    URL_PREFIX: /service/linkrecommendation/
    GUNICORN_TIMEOUT: 30
