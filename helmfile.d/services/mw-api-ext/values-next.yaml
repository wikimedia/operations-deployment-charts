resources:
  # T383845 - 88 replicas is 40% of the size of main (220) as of 2025-02-28.
  replicas: 88

# TODO: T383845 - Temporarily increase maxUnavailable. Needs revised down as
# the deployment grows.
strategy:
  rollingUpdate:
    maxUnavailable: 10%

mesh:
  public_port: 4455
  tracing:
    service_name: mw-api-ext-next

php:
  version: "8.1"
