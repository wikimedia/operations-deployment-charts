resources:
  # T383845 - 33 replicas is 15% of the size of main (220) as of 2025-01-24.
  replicas: 33

# TODO: T383845 - Temporarily increase maxUnavailable. Needs revised down as
# the deployment grows.
strategy:
  rollingUpdate:
    maxUnavailable: 20%

mesh:
  public_port: 4455
  tracing:
    service_name: mw-api-ext-next

php:
  version: "8.1"
