shellbox:
  flavour: score

service:
  deployment: production

mesh:
  enabled: true
  public_port: 4008
  tracing:
    enabled: true
    sampling: 10

resources:
  replicas: 7

config:
  public:
   FPM__log_level: debug
