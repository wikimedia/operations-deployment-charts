monitoring:
  enabled: false

inference_services:
  reference-need:
    predictor:
      config:
        minReplicas: 1
        maxReplicas: 1
  reference-risk:
    predictor:
      config:
        minReplicas: 1
        maxReplicas: 1
